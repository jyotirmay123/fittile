import { ProgressRing } from '../../design/components/ProgressRing'
export function NutritionSummary({kcal}:{kcal:number}){const target=2000;const macros=[['Protein',68,150,'g'],['Carbs',91,220,'g'],['Fat',24,67,'g'],['Fiber',18,30,'g']];return <section className="nutrition-summary"><div><p className="eyebrow">Daily budget</p><h2><strong>{kcal}</strong> / {target} kcal</h2><p>{Math.max(0,target-kcal)} kcal remaining</p></div><ProgressRing value={Math.round((kcal/target)*100)} label="Calories consumed" size={88} color="var(--brand)"/><div className="macro-stack">{macros.map(([name,value,max,unit])=><div key={name}><span>{name}</span><i><b style={{width:`${(Number(value)/Number(max))*100}%`}}/></i><strong>{value}{unit}</strong></div>)}</div></section>}
