export async function requireUser(request:Request){const authorization=request.headers.get('Authorization');if(!authorization)throw new Response(JSON.stringify({error:'authentication-required'}),{status:401});const url=Deno.env.get('SUPABASE_URL');const key=Deno.env.get('SUPABASE_ANON_KEY');const response=await fetch(`${url}/auth/v1/user`,{headers:{Authorization:authorization,apikey:key??''}});if(!response.ok)throw new Response(JSON.stringify({error:'invalid-session'}),{status:401});return response.json() as Promise<{id:string;email?:string}>}
