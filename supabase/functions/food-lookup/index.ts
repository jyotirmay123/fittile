import { normalizeOpenFoodFacts } from './normalize.ts'
Deno.serve(async(request)=>{if(request.method==='OPTIONS')return new Response('ok',{headers:{'Access-Control-Allow-Origin':'*','Access-Control-Allow-Headers':'authorization, content-type'}});const url=new URL(request.url);const barcode=url.searchParams.get('barcode')?.replace(/\D/g,'');if(!barcode)return Response.json({error:'barcode-required'},{status:400});const response=await fetch(`https://world.openfoodfacts.org/api/v3/product/${barcode}?fields=code,product_name,brands,serving_size,nutriments`,{headers:{'User-Agent':'Fitile/1.0 (personal health app)'}});if(!response.ok)return Response.json({error:'food-not-found'},{status:404});const payload=await response.json();return Response.json(normalizeOpenFoodFacts(payload),{headers:{'Access-Control-Allow-Origin':'*'}})})
